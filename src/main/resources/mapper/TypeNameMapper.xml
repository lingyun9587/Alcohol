<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.alcohol.mapper.type.TypeNameMapper">
    <!--查询三级分类-->
    <select id="getTypeName" resultType="TypeName">
SELECT typeName_id,typeName_name FROM tb_typename
WHERE category_id =(
	SELECT categorythree_id FROM tb_categorythree WHERE categorythree_id=2
)
    </select>

</mapper>