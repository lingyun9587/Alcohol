<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.alcohol.mapper.type.TypeValueMapper">
    <!--查询三级分类-->
    <select id="getTypeValue" resultType="Typevalue">
SELECT typevalue_id,typeValue_name FROM tb_typevalue
WHERE typeName_id=(
	SELECT typeName_id FROM tb_typename WHERE category_id=2
)
    </select>

</mapper>